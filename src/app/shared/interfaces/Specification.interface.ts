import { FormControl } from '@angular/forms';

export interface SpecificationInterface {
  m2: FormControl;
  roomsNumber: FormControl;
  bathroomsNumber: FormControl;
  deposit: FormControl;
  depositMonths: FormControl;
}
